{% set version = "1.1.2" %}

package:
  name: pytest-forked
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/p/pytest-forked/pytest-forked-{{ version }}.tar.gz
  sha256: 6d1c9052f5dd766a7cb2f8947fef7d32b6bbaf9aaea8cd971a1f84d257253498

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"

requirements:
  host:
    - python
    - pip
    - pytest >=2.6.0
    - setuptools_scm
  run:
    - python
    - pytest >=2.6.0

test:
  imports:
    - pytest_forked
  files:
    - dummy_test.py
  commands:
    - py.test --forked dummy_test.py  # [not win]

about:
  home: https://github.com/pytest-dev/pytest-forked
  license: MIT
  license_file: LICENSE
  summary: 'run tests in isolated forked subprocesses'
  dev_url: https://github.com/pytest-dev/pytest-forked

extra:
  recipe-maintainers:
    - nicoddemus
